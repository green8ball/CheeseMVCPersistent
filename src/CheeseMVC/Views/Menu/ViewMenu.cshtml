@model CheeseMVC.ViewModels.ViewMenuViewModel
@{
    //ViewBag.Title = Model.Menu.Name;
}
<ul>
    @foreach (var cheeseMenu in Model.Items)
    {
        <li>
            @cheeseMenu.Cheese.Name
        </li>
    }
</ul>

@*@if (Model.Menu.CheeseMenus.Count == 0)
{
    <p>No cheeses yet!</p>
}*@

<p><a asp-controller="Menu" asp-action="AddItem" asp-route-id=@Model.Menu.ID>Add Cheese to this Menu</a></p>

